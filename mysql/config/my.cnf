[mysqld]
# 외부 접속 가능하게 설정
bind-address=0.0.0.0
# 기본 문자열 세팅 utf8mb4는 이모지도 저장가능(4바이트까지 저장 가능)
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci
# max packet 설정
max_allowed_packet= 4M


[client]
default-character-set=utf8mb4